// Palette AviConnect — voir constants/theme.ts pour l'ensemble du design system
// (typographie, espacements, rayons, ombres). Ce fichier reste pour compat des imports existants.
export { Colors } from './theme';
